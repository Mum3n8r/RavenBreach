using UnityEngine;

namespace RavenbreachMod
{
    /// <summary>
    /// Replaced by SprintStumbleSystem.
    /// Speed multiplier penalties removed — suppression now punishes sprinting
    /// specifically via exponential stumble risk rather than flat slowdown.
    /// This component kept as a stub so Plugin.cs doesn't need changes.
    /// </summary>
    public class SuppressionMechanics : MonoBehaviour { }
}
